{
    "version": 2,
    "buildCommand": "node scripts/check-env.js && node scripts/generate-build-info.js && next build",
    "devCommand": "next dev",
    "installCommand": "npm install",
    "framework": "nextjs",
    "github": {
        "silent": true
    }
}